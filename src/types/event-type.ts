export enum EventType {
  ServerConnected,
  ServerNotification,
  ServerDisconnected,
  MatchAdvertised,
  PlayerConnected,
  HostDisconnected,
  PlayerDisconnected,
  Countdown,
  GoalStart,
  GoalEnd,
  GameOverStart,
  GameOverEnd,
}
